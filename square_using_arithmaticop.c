// program to find the square of a number using an arithmetic operator. 
#include<stdio.h>
int main()
{
    int num1,square;
    printf("enter a number:\n");
    scanf("%d",&num1);
    square=num1*num1;
    printf("the square of a number=%d",square);
    return 0;
}